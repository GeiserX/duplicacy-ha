# AGENTS.md — Duplicacy HA

## Project Overview

Home Assistant custom integration for monitoring Duplicacy backups via Prometheus exporter.

- **Repo:** [GeiserX/duplicacy-ha](https://github.com/GeiserX/duplicacy-ha)
- **Language:** Python
- **License:** GPL-3.0
- **Parent project:** [GeiserX/duplicacy-exporter](https://github.com/GeiserX/duplicacy-exporter)

## Architecture

Home Assistant custom integration (HACS-compatible). Communicates with the duplicacy-exporter API to expose sensors and services in Home Assistant.

### Key Files

- `custom_components/duplicacy_ha/` — Integration directory
  - `__init__.py` — Setup and config entry
  - `manifest.json` — HA integration manifest
  - `sensor.py` — Sensor entities
  - `config_flow.py` — UI configuration flow

## Installation

Install via HACS or copy `custom_components/` to your HA config directory.

## Security

> **Do not commit secrets to the repository.**
> Use environment variables or secure vaults for credentials.

---

*Generated by [LynxPrompt](https://lynxprompt.com) CLI*
